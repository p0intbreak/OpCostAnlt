"""Helpers for building analytical data marts."""

import pandas as pd


def build_monthly_spend_mart(dataframe: pd.DataFrame) -> pd.DataFrame:
    """Aggregate spend by reporting period using a placeholder implementation."""
    return dataframe.copy()

